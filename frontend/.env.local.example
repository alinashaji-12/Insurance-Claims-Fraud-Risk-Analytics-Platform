# Local development
NEXT_PUBLIC_API_URL=http://localhost:8000

# Production (Render): public URL of claimguard-api, no trailing slash
# NEXT_PUBLIC_API_URL=https://claimguard-api.onrender.com
# Note: this is baked in at `next build` — change it, then redeploy the frontend.
