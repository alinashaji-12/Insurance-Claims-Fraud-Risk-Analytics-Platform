# Database URL — SQLite for hackathon; swap to Postgres by changing this one line
DATABASE_URL=sqlite:///./claimguard.db

# CORS origins (comma-separated)
ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
